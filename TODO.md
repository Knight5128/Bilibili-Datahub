# 文档目的

此文档只用作我-User的个人笔记及待办备忘录，任何Agent在浏览到此文档时直接略过即可。

# 我的待办清单

- bilibili video pool builder的后续改进：

自定义抓取模块中的流程：
                ——>>  抓取热门400条 & n期每周必看 & 全部分区的当天主流视频
                    (每一次保存为full_site_floorings\目录下的一个csv)
                    (待添加选择爬取三者的哪几种的选项，并重设命名逻辑。例如，用户可以选择只抓取热门 or 只抓取过去n周的每周必看 or 全部分区的当天主流视频 or 任意两者的组合 or 三者都抓。相应的可以在命名中体现为：daily_hot、weeklymustsee_n、column_top) 🟨
                ——>> bvid 转 uid
     （待增逻辑）    (每一次保存为full_site_floorings\目录下，与上面的视频列表同时间戳的一个csv：<重设后的命名逻辑>_authors.csv) 🟨
    (已实现)    ——>> uid_expansion: 手动上传刚才得到的uid List(最新的..._authors.csv)，抓取每个uid的start_date到end_date（默认到系统日期的今天）内上传的所有视频，并循环增量输出视频列表和剩余uids列表
    (待增功能)  ——>> 初次开始一次新的uid_expansion任务时将一开始上传的uid列表自动保存为新任务目录中的original_uids.csv，新任务目录的命名中也要将<lookback_days>替换为<start_date>（此次任务中爬取到的最早的视频发布日期）和<end_date>（此次任务中爬取到的最晚的视频发布日期） 🟨
(待新增重要逻辑) ——>> 对于其中的每个uid：
                    若其在之前的任务中（之前所有的uid_expansion任务的original_uids.csv中）出现过，这只抓取到最后一次的时间节点；
                    若其在之前的任务中从未出现过，则默认抓取到过去固定的一个时间节点——————在全局设置中可以设置start_date，默认为：2025年9月23日星期二 蛇年八月初二————————注意。这意味着之前我们所定义的所有lookback_days的相关逻辑都用start_date来替代(默认为2025/09/23)，以便统一抓取视频的时间窗口；折页机意味着，在保存csv文件时（bvid/uid），其命名时间戳只截止到开始和结束的日期即可（年份用后两位数表示即可，即用26表示2026，用25表示2025）但是一次任务的开始/结束的精确时间还是要体现在log中的
    (待增功能)  ——>> 在“自定义抓取”的全局设置中：额外添加一个end_date选项（默认为到系统时间的现在的日期）：抓取start_date到end_date之间发布的视频列表🟨

最终想要得到的效果就是/uid_expansions/目录下的所有任务下的所有videolists都两两没有重复的视频条目

    🤓🤓（这样就能保证几乎不会重复爬取一个作者的同一个视频，还能大幅加速爬取视频列表进度，更能****快速得知一批作者的最新发布的视频****，
    稳定后就可以直接依次将videolist放到data crawler中，实现bucket中的数据纯增量更新（前提是crawler的数据列已经足够全面，
    诶，即使有改动，videolist也是增量的，且越后面得到的视频越新）
    我真是天才[得意]）

    - ✅去重模块
    - ✅新模块A:上传作者uid csv，抓取他最近k天的视频
    - ✅新模块B：上传视频uid csv，抓取其作者近k天的所有视频
    - ✅单bvid/uid网页链接跳转(用于快速验证有效性)
    - 🟥新模块C（初步无需构建，会引入个人bias）：直接通过UI的方式抓取单一用户（比如我）的关注作者列表（配合模块A）、收藏的所有视频列表（配合模块B）
    - 🟨作者owner_mid列表取 差集 功能，在uid扩展时就不用重复吧之前抓过的作者再抓一遍了
    - 🟨在log中加入“本次抓取视频列表任务起始时间、结束时间、总耗时”

- bilibili video data crawler的后续改进：
    - ✅单bvid网页链接跳转
    - ✅把对于一个bvid所实时抓取的所有属性都列出来
    - ✅抓取字段的定义.md文档+前端页面（先定义好，后面要**持续更新**）
    - ✅选好数据库
    - ❌设置视频下载长度截断
    - ✅探索bilibili-api可获取的更多视频元数据及上传者个人元数据
    - 🟨在log中加入“本次爬取视频数据任务起始时间、结束时间、总耗时”
    - 🟨探索能否获取视频在 过去 时间节点上的互动量!!!!!!!!!!!!!!

# 补充-B站url格式

- 单一视频播放: https://www.bilibili.com/video/<bvid>
- 单个用户/UP主个人主页: https://space.bilibili.com/<owner_mid>
- 单个UP主的视频列表: https://space.bilibili.com/<owner_mid>/upload/video